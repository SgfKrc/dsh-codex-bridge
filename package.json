{
  "name": "dsh-subagent-bridge",
  "version": "0.2.0",
  "private": true,
  "description": "Zero-dependency MCP stdio bridge that publishes the DSH headless/ACP harness as a Codex-callable subagent tool, so DSH and Reasonix can be compared as subagent harnesses on the same model.",
  "type": "module",
  "engines": { "node": ">=20" },
  "license": "MIT",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/server.mjs && node --check src/acp-client.mjs"
  },
  "files": [
    "src",
    "test",
    "scripts",
    "acp-route.patch.yml",
    "README.md"
  ]
}
